package com.example.vinilos.data.network

/**
 * Interface principal de la API de Vinilos
 * Aquí se definen todos los endpoints de la API
 * 
 * Ejemplo de uso:
 * interface ApiService {
 *     @GET("albums")
 *     suspend fun getAlbums(): Response<List<Album>>
 *     
 *     @GET("albums/{id}")
 *     suspend fun getAlbumById(@Path("id") id: Int): Response<Album>
 * }
 */
interface ApiService {
    // TODO: Definir los endpoints de la API
    // Los métodos se agregarán según se necesiten
}

